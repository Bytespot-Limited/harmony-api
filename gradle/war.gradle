apply plugin: "war"

bootWar {
    mainClass = "tech.bytespot.harmony.HarmonyApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
